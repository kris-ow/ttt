Show pipeline cost summary.

## Steps

1. Run `npm run costs` to generate the cost summary table.

2. If that fails, read `scripts/pipeline/costs.json` directly and compute:
   - Total cost
   - Cost per day (last 7 days)
   - Average cost per summary
   - Number of summaries generated

3. Report a concise cost breakdown table.
