# Backlog

## Tooling / DX
- [ ] **Latency report: livestream-aware lag.** `latency-report.js` measures from `published_utc`, but for livestreams/premieres that's the *scheduled start* — a 9h stream shows as 9h "lag" even when the transcript was fetched minutes after the VOD existed (Mac Mini diagnosis 2026-06-12: all 21 tail videos were streams/premieres). Also, RSS entries without timestamps synthesize `T00:00:00`, faking up to 24h of lag (electrified 05-26). Consider having yt-transcripts record stream-end / VOD-available time in the transcript header and measuring the fetch leg from that.
- [ ] **SessionStart hook for `git pull`** — add a hook in `settings.json` (via `update-config` skill) so the harness auto-syncs the repo at session start. More reliable than relying on Claude reading the `feedback_git_pull` memory. Consider `git fetch` + a notice when behind (safer than `git pull` when local changes exist).

## Content / SEO
- [ ] **(conditional) Weekly Brief as a deep-link post, like interviews.** IF the 2026-07-01 pure-embed interview post performs on r/teslainvestorsclub (not downvoted / mod-removed — see the audience-growth follow-up), extend the interview deep-link + OG-card pattern to the **Weekly Tesla Brief**: a shareable page (e.g. `/w/<date>/`) with its own OG card, posted to Reddit as a Link Embed. Would replace or A/B against the current text-only `reddit-weekly.js` copy-paste post. Scope note: briefs are **feed articles** (ArticleDetail), not interviews — needs article/brief deep-link routing + static OG shell + card generation (bigger than the interview case; likely scope to just the weekly brief, not every daily). Fix the Reddit thumbnail-crop item below first so the brief card isn't clipped too.
- [ ] **(med) Interview OG card is clipped on Reddit's thumbnail crop.** The 1200×630 (1.91:1) card renders fully in expanded view / Discord / X large card, but Reddit's compact/feed thumbnail **center-crops toward a square**, cutting the left/right edges. Because the card is left-aligned, the person's name gets clipped (user saw only "…s Moravy" for "Lars Moravy", 2026-07-01). Fix: keep brand + name inside a **center-safe zone** (roughly the central 630px square; avoid the outer ~285px each side) — either center-align the key elements or pull the name in from the left edge. Eyeball variants; verify against the actual Reddit thumbnail after the change. `scripts/build-og-images.js`.
- [ ] **(low) Punchier interview description hook.** The `/i/<slug>/` `og:description` + `meta description` are auto-sliced from the summary's opening paragraph — correctly sized now but often dry (e.g. "…sat down with Herbert and supply chain executive Jeff Lutz…"). This text is the Google SERP snippet and the X/Discord/Slack link-preview blurb; it is **not** prominent on Reddit (Reddit's card is image + title + domain), so title + OG image carry Reddit and the hook only really helps Google + X/Discord. If worth it: add an authored one-line `Teaser:` field to the interview file header (hand-written or generated by `interview-summary.js`) that `build-interview-pages.js` prefers over the paragraph slice. Low priority.

## Pipeline resilience
- [ ] **External trigger for the Monday weekly-brief run.** GitHub Actions cron is best-effort across all plan tiers — the shared 07:15 UTC slot consistently slipped 3–4h on Mondays. On 2026-06-01 the weekly brief was given its own Monday-only `45 5 * * 1` cron (off the :00/:15 stampede) so it fires fast without dragging the daily exec-summary 2nd pass off 07:15. If 05:45 also drifts unacceptably, add a Mac Mini cron that hits the `dispatches` API at exact UTC time. Same backstop could apply to the daily 00:15 slot if it ever starts drifting.
